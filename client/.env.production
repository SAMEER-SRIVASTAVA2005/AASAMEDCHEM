# In production on Vercel, both frontend and API are on the same domain.
# The vercel.json routes /api/* to the Express function, so we use /api directly.
# No need to change baseURL — the default '/api' in axios.js works perfectly.
VITE_API_BASE_URL=/api
